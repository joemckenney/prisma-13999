{
  "name": "orm.packages.users.services",
  "version": "0.0.0",
  "description": "Contains the users's prisma client",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "browser": "./dist/index-browser.js",
  "scripts": {
    "👇required package scripts": "",
    "build": "echo \"orm.packages.users.services does not require build\"; exit 0;",
    "clean": "shx rm -rf ./dist",
    "dev": "echo \"orm.packages.users.services does not require dev\"; exit 0;",
    "format": "echo \"orm.packages.users.services does not require format\"; exit 0;",
    "formatcheck": "echo \"orm.packages.users.services does not require formatcheck\"; exit 0;",
    "lint": "echo \"orm.packages.users.services does not require lint\"; exit 0;",
    "test": "echo \"orm.packages.users.services does not require test\"; exit 0;",
    "typecheck": "echo \"orm.packages.users.services does not require typecheck\"; exit 0;",
    "☝️ required package scripts": ""
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "shx": "0.3.4"
  },
  "dependencies": {
    "schema.packages.users.services": "workspace:*"
  }
}
